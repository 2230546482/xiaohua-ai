PAQ_y_game = {
	allowed = { 
		tag = PAQ
	}

	enable = {
		tag = PAQ
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = equipment_stockpile_surplus_ratio
		value = -80 # reduce while we build up troops for africa etc
	}
}

PAQ_you_should_be_prepared_for_war = {
	allowed = { 
		tag = PAQ
	}
	enable = {
		always = yes
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
        type = force_build_armies
        value = 100
    }
}

PAQ_time_to_war = {
	allowed = {
		original_tag = PAQ
	}
	enable = {
		date > 1936.3.1
		country_exists = PA1
		OR = {
			has_war = no
			has_war_with = PA1
		}
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = prepare_for_war
		id = PA1
		value = 300
	}
}

PAQ_war_with_PA1 = {
	allowed = {
		original_tag = PAQ
	}
	enable = {
		has_war_with = PA1
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = invade		#入侵
		id = "PA1"			
		value = 200
	}
	ai_strategy = {
		type = conquer		#征服
		id = "PA1"			
		value = 200
	}
	ai_strategy = {
		type = invasion_unit_request
		tag = PA1
		value = 0
	}
	
	ai_strategy = {
		type = front_unit_request #前线部队
		tag = PA1
		value = 200 # lets put more units, should be safe
	}

	ai_strategy = {
		type = front_control

		tag = PA1

		ratio = 1

		priority = 100
		ordertype = front
		execution_type = rush
		execute_order = yes
	}
	
}

PAQ_time_to_war_2 = {
	allowed = {
		original_tag = PAQ
	}
	enable = {
		date > 1936.8.1
		not = { country_exists = PA1 }
		country_exists = ELF
		OR = {
			has_war = no
			has_war_with = ELF
		}
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = prepare_for_war
		id = ELF
		value = 300
	}
}

PAQ_war_with_ELF = {
	allowed = {
		original_tag = PAQ
	}
	enable = {
		has_war_with = ELF
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = conquer		#征服
		id = "ELF"			
		value = 200
	}
	
	ai_strategy = {
		type = front_unit_request #前线部队
		tag = ELF
		value = 200 # lets put more units, should be safe
	}

	ai_strategy = {
		type = front_control

		tag = ELF

		ratio = 1

		priority = 100
		ordertype = front
		execution_type = rush_weak
		execute_order = yes
	}
	
}

PAQ_war_with_MGU_or_LHS = {
	allowed = {
		original_tag = PAQ
	}
	enable = {
		OR = {
			has_war_with = MGU
			has_war_with = LHS
			has_war_with = PAM
		}
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = front_control

		tag = LHS

		ratio = 0.6

		priority = 100
		ordertype = front
		execution_type = balanced
		execute_order = yes
	}

	ai_strategy = {
		type = front_control

		tag = PAM

		ratio = 0.6

		priority = 100
		ordertype = front
		execution_type = balanced
		execute_order = yes
	}

	ai_strategy = {
		type = front_control

		tag = MGU

		ratio = 0.8

		priority = 100
		ordertype = front
		execution_type = rush
		execute_order = yes
	}

	ai_strategy = {
		type = front_control

		tag = LFC

		ratio = 0.8

		priority = 100
		ordertype = front
		execution_type = rush
		execute_order = yes
	}
	
}
