AI_start1 = {
    enable = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = front_control
        state_trigger = {
            is_controlled_by = ROOT
            any_neighbor_state = { controller = { has_war_with = ROOT } }
            NOT = { is_coastal = yes }
        }
        priority = 200
        ordertype = front
        execution_type = balanced
        manual_attack = yes
    }
    ai_strategy = { #注重船坞
        type = front_control
        state_trigger = {
            is_coastal = yes
            FROM = { has_war_with = FROM.FROM }
        }
        priority = 120
        ordertype = front
        execution_type = rush
        manual_attack = yes

    }
    ai_strategy = {
        type = building_target
        id = supply_node
        value = 200
    }
    ai_strategy = { #36年步兵装备
        type = research_tech
        id = infantry_weapons1
        value = 2000
    }
    ai_strategy = {
        type = research_tech	#工业
        id = concentrated_industry
        value = 200
    }
    ai_strategy = {
        type = research_tech	#电子学
        id = electronic_mechanical_engineering
        value = 200
    }
    #间谍
    ai_strategy = {
        type = become_spymaster
        value = 30
    }
    ai_strategy = {
        type = intelligence_agency_branch_desire_factor
        id = branch_crypto  #密码学
        value = 2000
    }
    ai_strategy = {
        type = intelligence_agency_branch_desire_factor
        id = branch_defense  #反间谍
        value = 1500
    }
    ai_strategy = {
        type = garrison
        value = -200  #减少无意义驻军
    }
    #经验值使用
    ai_strategy = {
        type = land_xp_spend_priority
        id = division_template  #优先升级编制
        value = 200
    }
    ai_strategy = {
        type = land_xp_spend_priority
        id = upgrade_xp_cutoff  #然后升学说
        value = 100
    }
}
the_more_powerful_jiandie = {
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        always = yes
    }

    abort = {
        always = no
    }

    ai_strategy = {
        type = intelligence_agency_usable_factories
        value = 1000
    }

    ai_strategy = {
        type = intelligence_agency_branch_desire_factor
        id = branch_defense
        value = 1500
    }
    ai_strategy = {
        type = intelligence_agency_branch_desire_factor
        id = branch_crypto
        value = 2000
    }

    ai_strategy = {
        type = operation_equipment_priority
        value = 100
    }

}

moren_More_Infantry_focus = {#多产枪（40~70工厂）
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        arms_factory > 39
        arms_factory < 71
        has_equipment = { infantry_equipment < 1000 }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = equipment_production_factor
        id = infantry
        value = 50
    }
}

moren_More_Infantry_focus_2 = {#多产枪（71~100工厂）
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        arms_factory > 70
        arms_factory < 101
        has_equipment = { infantry_equipment < 1000 }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = equipment_production_factor
        id = infantry
        value = 75
    }
}

moren_More_Infantry_focus_3 = {#多产枪（101+工厂）
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        arms_factory > 100
        has_equipment = { infantry_equipment < 1000 }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = equipment_production_factor
        id = infantry
        value = 125
    }
}

moren_More_Support_focus = {#多产支援（40~70工厂）
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        arms_factory > 39
        arms_factory < 71
        has_equipment = { support_equipment < 100 }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = equipment_variant_production_factor
        id = support_equipment
        value = 25
    }
}

moren_More_Support_focus_2 = {#多产支援（71~100工厂）
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        arms_factory > 70
        arms_factory < 101
        has_equipment = { support_equipment < 100 }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = equipment_variant_production_factor
        id = support_equipment
        value = 50
    }
}

moren_More_Support_focus_3 = {#多产支援（101+工厂）
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        arms_factory > 100
        has_equipment = { support_equipment < 100 }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = equipment_variant_production_factor
        id = support_equipment
        value = 100
    }
}


moren_stop_disbanding_your_army = {#ai部署陆军倾向（扩军期）
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        num_divisions < 700
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = ai_wanted_divisions_factor
        value = 800
    }
} 
moren_More_Infantry_focus_2 = {
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        arms_factory > 70
        arms_factory < 99
        has_equipment = { infantry_equipment < 1000 }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = equipment_production_factor
        id = infantry
        value = 75
    }
    abort_when_not_enabled = yes
}



moren_More_Infantry_focus_3 = {
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        arms_factory > 100
        has_equipment = { infantry_equipment < 1000 }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = equipment_production_factor
        id = infantry
        value = 125
    }
}




moren_More_Support_focus = {
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        arms_factory > 40
        arms_factory < 69
        has_equipment = { support_equipment < 100 }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = equipment_variant_production_factor
        id = support_equipment
        value = 25
    }
}


moren_More_Support_focus_2 = {
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        arms_factory > 70
        arms_factory < 99
        has_equipment = { support_equipment < 100 }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = equipment_variant_production_factor
        id = support_equipment
        value = 50
    }
}
moren_More_Support_focus_3 = {
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        arms_factory > 100
        has_equipment = { support_equipment < 100 }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = equipment_variant_production_factor
        id = support_equipment
        value = 100
    }
}
LessAirForMass = {
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        has_doctrine = mass_assault
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type  = air_factory_balance
        value = -25
    }
}
#多下兵

AI_xiabing = {
    allowed = {
        NOT = {
            original_tag = LHS
        }
    }
    enable = {
        always = yes
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = force_build_armies
        value = 100
    }
}

youhavethemusethemmofa = {
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        has_idea = mofa
        date > 1938.1.1
        or = {
            has_equipment = { medium_tank_chassis > 1 }
            has_equipment = {medium_tank_destroyer_chassis > 1}
        }
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = build_army
        id = special_armor
        value = 100
    }

    ai_strategy = {
        type = template_prio
        id = special_armor
        value = 100
    }

    ai_strategy = {
        type = role_ratio
        id = special_armor
        value = 800
    }

}
youhavethemusethem2 = {
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        not = {
            has_idea = mofa
        }
        date > 1938.1.1
        or = {
            has_equipment = { medium_tank_chassis > 1 }
            has_equipment = {medium_tank_destroyer_chassis > 1}
        }
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = build_army
        id = armor
        value = 100
    }

    ai_strategy = {
        type = template_prio
        id = armor
        value = 100
    }

    ai_strategy = {
        type = role_ratio
        id = armor
        value = 800
    }

}



Ifyouhavethemusethem3 = {
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        date < 1938.1.1
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = template_prio
        id = infantry
        value = 100
    }
    ai_strategy = {
        type = role_ratio
        id = infantry
        value = 50
    }
    ai_strategy = {
        type = build_army
        id = infantry
        value = 100
    }
}

#造列车炮
zao_liechepao = {
    allowed = { always = yes }
    enable = {
        num_divisions > 200
        num_of_military_factories > 200
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = railway_gun_divisions_ratio
        value = 1
    }
    ai_strategy = {
        type = equipment_production_min_factories_archetype
        id = railway_gun_equipment
        value = 5
    }
}
moren_avoid_thin_lines = {
    allowed = { NOT = {
        OR = {
            original_tag = PAO
            original_tag = PAM
            original_tag = ELF
            original_tag = KNM
            original_tag = KFL
            original_tag = LHS
        }
    } }
    enable = {
        has_war = yes
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = front_unit_request
        state_trigger = {
            is_controlled_by = ROOT
            check_variable = {
                num_enemy_neighbor_states > 2
            }
        }
        value = 100
    }


}
ai_research_mechanised = { #机械化1
    allowed = {
    }
    enable = {
        date > 1940.1.1
    }
    abort = {
        always = no
    }
    ai_strategy = { #机械化1
        type = research_tech
        id = mechanised_infantry
        value = 800
    }
}
ai_research_mechanised_2 = { #机械化2
    enable = {
        date > 1942.1.1
    }
    abort = {
        always = no
    }
    ai_strategy = { #机械化2
        type = research_tech
        id = mechanised_infantry2
        value = 800
    }
}
ai_research_mechanised_3 = { #机械化3
    allowed = {
    }
    enable = {
        date > 1944.6.1
    }
    abort = {
        always = no
    }
    ai_strategy = { #机械化3
        type = research_tech
        id = mechanised_infantry3
        value = 800
    }
}


moren_motorized_production = { #缺卡车
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        has_equipment = {
            motorized_equipment < 1000
        }
        num_of_military_factories > 50
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories
        id = motorized
        value = 10
    }
    ai_strategy = {
        type = equipment_variant_production_factor
        id = motorized_equipment
        value = 50
    }
}
moren_artillery_production = { #缺炮
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }

    }
    enable = {
        has_equipment = {
            artillery_equipment < 200
        }
        num_of_military_factories > 50
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories
        id = artillery
        value = 5
    }
}
moren_anti_tank_production = { #缺at
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }

    }
    enable = {
        has_equipment = {
            anti_tank_equipment < 100
        }
        num_of_military_factories > 50
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories
        id = anti_tank
        value = 5
    }
}
moren_anti_air_production = { #缺对空
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }

    }
    enable = {
        has_equipment = {
            anti_air_equipment < 200
        }
        num_of_military_factories > 50
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories
        id = anti_air
        value = 5
    }
}
moren_train_production = { #缺火车
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }

    }
    enable = {
        has_equipment = {
            train_equipment  < 50
        }
        num_of_military_factories > 50
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories
        id = train
        value = 8
    }
}
moren_convoy_production = { #缺运输船
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }

    }
    enable = {
        has_equipment = {
            convoy  < 500
        }
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories
        id = convoy
        value = 10
    }
}
AI_late_game_warrior = {
    allowed = {  NOT = {
        OR = {
            original_tag = PAO
            original_tag = PAM
            original_tag = ELF
            original_tag = KNM
            original_tag = KFL
            original_tag = LHS
        }
    } }
    enable = {
        date > 1938.1.1
    }
    abort_when_not_enabled = yes
    # 军工优先
    ai_strategy = {
        type = building_target
        id = arms_factory
        value = 300
    }
    ai_strategy = {
        type = building_target
        id = dockyard
        value = 100
    }
    ai_strategy = {
        type = added_military_to_civilian_factory_ratio
        value = 99
    }
}
AI_game_warrior = {
    allowed = {  NOT = {
        OR = {
            original_tag = PAO
            original_tag = PAM
            original_tag = ELF
            original_tag = KNM
            original_tag = KFL
            original_tag = LHS
        }
    } }
    enable = {
        date < 1938.1.1
    }
    abort_when_not_enabled = yes
    #民工优先
    ai_strategy = {
        type = building_target
        id = industrial_complex
        value = 300
    }
    ai_strategy = {
        type = added_military_to_civilian_factory_ratio
        value = -99
    }

}
#步兵装备
moren_production_tier1_infantry = {
    allowed = { NOT = {
        OR = {
            original_tag = PAO
            original_tag = PAM
            original_tag = ELF
            original_tag = KNM
            original_tag = KFL
            original_tag = LHS
        }
    } }
    enable = {
        has_equipment = { infantry_equipment < 10000 }
        num_of_military_factories > 20
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_factor
        id = infantry
        value = 200
    }

    ai_strategy = {
        type = equipment_production_min_factories
        id = infantry
        value = 15  #至少15个厂
    }
}

#支援装备、火炮
moren_production_tier2_support = {
    allowed = { NOT = {
        OR = {
            original_tag = PAO
            original_tag = PAM
            original_tag = ELF
            original_tag = KNM
            original_tag = KFL
            original_tag = LHS
        }
    } }
    enable = {
        has_equipment = { infantry_equipment > 5000 }#枪够了
        has_equipment = { support_equipment < 500 }
        num_of_military_factories > 30
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_factor
        id = support
        value = 150
    }

    ai_strategy = {
        type = equipment_production_min_factories
        id = support
        value = 5
    }
}


moren_counter_enemy_armor = { #应对敌军装甲
    allowed = { NOT = {
        OR = {
            original_tag = PAO
            original_tag = PAM
            original_tag = ELF
            original_tag = KNM
            original_tag = KFL
            original_tag = LHS
        }
    } }
    enable = {
        has_war = yes
        OR = {
            any_enemy_country = {
                or = {
                    has_equipment = {
                        light_tank_chassis > 2000
                    }
                    has_equipment = {
                        heavy_tank_chassis > 2000
                    }
                    has_equipment = {
                        medium_tank_chassis > 2000
                    }
                }
            }
            any_enemy_country = {
                has_tech = improved_medium_tank_chassis
                num_of_military_factories > 40
            }
        }
    }
    abort_when_not_enabled = yes

    #爆反坦炮
    ai_strategy = {
        type = equipment_production_factor
        id = anti_tank
        value = 200
    }
    ai_strategy = {
        type = equipment_production_min_factories
        id = anti_tank
        value = 15
    }
    #前线反坦炮优先权
    ai_strategy = {
        type = front_unit_request
        state_trigger = {
            controller = { has_war_with = ROOT }
            any_neighbor_state = {
                is_controlled_by = ROOT
            }
        }
        value = 30
    }
    #研究反坦科技
    ai_strategy = {
        type = research_tech
        id = antitank2
        value = 500
    }
    ai_strategy = {
        type = research_tech
        id = antitank3
        value = 500
    }
}


moren_victory_acceleration = {#冲刺！！！
    allowed = {NOT = {
        OR = {
            original_tag = PAO
            original_tag = PAM
            original_tag = ELF
            original_tag = KNM
            original_tag = KFL
            original_tag = LHS
        }
    } }
    enable = {
        has_war = yes
        any_enemy_country = {
            surrender_progress > 0.2
        }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = front_control
        state_trigger = {
            controller = {
                has_war_with = ROOT
                surrender_progress > 0.2
            }
        }
        priority = 15000
        ordertype = front
        execution_type = rush_weak
        execute_order = yes
        manual_attack = yes
    }
    ai_strategy = {
        type = spare_unit_factor
        value = -30
    }
    ai_strategy = {
        type = garrison
        value = -200
    }
}

moren_target_enemy_supply_hubs = {#断补给
    allowed = { NOT = {
        OR = {
            original_tag = PAO
            original_tag = PAM
            original_tag = ELF
            original_tag = KNM
            original_tag = KFL
            original_tag = LHS
        }
    } }
    enable = {
        has_war = yes
        num_of_military_factories > 60
    }
    abort_when_not_enabled = yes

    #优先进攻有补给站的州
    ai_strategy = {
        type = front_control
        state_trigger = {
            controller = { has_war_with = ROOT }
            any_province_building_level = {
                province = { all_provinces = yes }
                building = supply_node
                level > 0
            }
        }
        priority = 6000
        ordertype = front
        execution_type = rush
        execute_order = yes
        manual_attack = yes
    }
    #专门集合大部队去打这些点
    ai_strategy = {
        type = front_unit_request
        state_trigger = {
            controller = { has_war_with = ROOT }
            any_province_building_level = {
                province = { all_provinces = yes }
                building = supply_node
                level > 0
            }
            any_neighbor_state = { is_controlled_by = ROOT }
        }
        value = 60
    }
}

moren_push_enemy_capital = {#冲首都
    allowed = { NOT = {
        OR = {
            original_tag = PAO
            original_tag = PAM
            original_tag = ELF
            original_tag = KNM
            original_tag = KFL
            original_tag = LHS
        }
    } }
    enable = {
        has_war = yes
        any_enemy_country = { surrender_progress > 0.1 }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = front_control
        state_trigger = {
            owner = { has_war_with = ROOT }
            is_capital = yes
            any_neighbor_state = { is_controlled_by = ROOT }
        }
        priority = 50000
        ordertype = front
        execution_type = rush
        execute_order = yes
        manual_attack = yes
    }
    ai_strategy = {
        type = front_unit_request
        state_trigger = {
            owner = { has_war_with = ROOT }
            is_capital = yes
        }
        value = 300
    }
    ai_strategy = {
        type = invasion_unit_request
        state_trigger = {
            controller = { has_war_with = ROOT }
            is_coastal = yes
            any_province_building_level = {
                province = { all_provinces = yes }
                building = supply_node
                level > 0
            }
        }
        value = 50
    }
}
FRA_support_ROC = {
    reversed = yes
    allowed = {
        original_tag = FRA
    }
    enable = {
        tag = ROC
        or = {
            has_government = democratic
            has_government = neutrality
        }
        FRA = {
            or = {
                has_government = democratic
                has_government = neutrality
            }
        }
    }
    ai_strategy = {
        type = send_volunteers_desire
        id = "FRA"
        value = 1000
    }
    abort_when_not_enabled = yes
}
SOV_send_volunteers = { # Reverse strategy

    reversed = yes

    allowed = {
        original_tag = SOV
    }

    enable = {


        if = {
            limit = { NOT = { has_completed_focus = SOV_Support_Viet_Minh } }
            # NOT = {
            # 	tag = CAM
            # 	tag = VIN
            # }
        }
        OR = {
            has_country_flag = SWE_Communist_Party_Cabinet
            has_government = communism
        }
        NOT = {
            any_enemy_country = {
                has_government = communism
                is_subject = no
                OR = {
                    is_faction_leader = yes
                    is_in_faction = no
                }
            }
        }
        NOT = {
            has_country_leader_ideology = anarchist_communism
        }
        NOT = {
            capital_scope = {
                is_on_continent = africa
            }
        }
        if = {
            limit = { tag = PAN }
            always = yes
        }
        else_if = {
            limit = {
                UKR = {
                    exists = yes
                    NOT = { has_government =  communism }
                }
            }
            always = no
        }
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = send_volunteers_desire
        id = "SOV"
        value = 100
    }
}

SOV_send_lease_desire = { # Reverse strategy

    reversed = yes

    allowed = {
        original_tag = SOV
    }

    enable = {
        OR = {
            any_enemy_country = {
                tag = DVL
            }
            AND = {
                any_enemy_country = {
                    has_government = fascism
                }
                NOT = { tag = DVL }
            }
            AND = {
                NOT = { country_exists = DVL }
                any_enemy_country = {
                    tag = ENG
                }
            }
        }
        NOT = {
            any_enemy_country = {
                has_government = communism
                is_subject = no
                OR = {
                    is_faction_leader = yes
                    is_in_faction = no
                }
            }
        }
        if = {
            limit = {
                tag = GER
            }
            has_idea = SOV_aid_to_GER
        }
        else = {
            has_government = communism
        }
        NOT = { has_war_with = SOV }
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = send_lend_lease_desire
        id = "SOV"
        value = 100
    }
}
ENG_support_ROC = {
    reversed = yes
    allowed = {
        original_tag = ENG
    }
    enable = {
        tag = ROC
        or = {
            has_government = democratic
            has_government = neutrality
        }
        ENG = {
            or = {
                has_government = democratic
                has_government = neutrality
            }
        }
    }
    ai_strategy = {
        type = send_volunteers_desire
        id = "ENG"
        value = 1000
    }
    abort_when_not_enabled = yes
}
 
moren_postwar_consolidation = {#战后恢复
    allowed = { NOT = {
        OR = {
            original_tag = PAO
            original_tag = PAM
            original_tag = ELF
            original_tag = KNM
            original_tag = KFL
            original_tag = LHS
        }
    } }
    enable = {
        has_war = no
        OR = {
            num_divisions < 300
            has_manpower < 1000000
        }
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_factor
        id = infantry
        value = 200
    }
    ai_strategy = {
        type = equipment_production_min_factories
        id = infantry
        value = 20
    }
    ai_strategy = {
        type = ai_wanted_divisions_factor
        value = 1500
    }
    ai_strategy = {
        type = building_target
        id = infrastructure
        value = 300
    }
    ai_strategy = {
        type = building_target
        id = supply_node
        value = 400
    }
    ai_strategy = {
        type = garrison
        value = 50
    }
}
moren_state_is_zhanlue = {#老爷爷，我给你踩地来咯
    allowed={
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        or = {
            any_enemy_country = {
                strength_ratio = {
                    tag = ROOT
                    ratio < 1.4
                }
            }
        }
    }
    abort = {
        and = {
            or = {
                any_enemy_country = {
                    strength_ratio = {
                        tag = ROOT
                        ratio > 1.4
                    }
                }
            }
        }

    }
    ai_strategy = {
        type = front_control
        state_trigger = {
            is_coastal = yes
            controller = { has_war_with = ROOT }
        }
        ratio = 0.2
        priority = 20000
        ordertype = front
        execution_type = rush
        execute_order = yes
        manual_attack = yes
    }
    ai_strategy = {
        type = front_control

        state_trigger = {
            FROM = { has_war_with = FROM.FROM }
            FROM = {
                divisions_in_state = { state = ROOT size = 0 }
            }
        }
        priority = 90000
        ordertype = front
        execution_type = rush_weak
        execute_order = yes
        manual_attack = yes
    }
    ai_strategy = {
        type = front_control

        state_trigger = {
            FROM = { has_war_with = FROM.FROM }
            FROM = {
                divisions_in_state = { state = ROOT size < 3 }
            }
        }

        ratio = 0.8

        priority = 5000
        ordertype = front
        execution_type = rush
        execute_order = yes
        manual_attack = yes
    }
    ai_strategy = {
        type = front_control
        state_trigger = {
            controller = { has_war_with = ROOT }
            OR = {
                any_province_building_level = {
                    province = { all_provinces = yes }
                    building = supply_node
                    level > 0
                }
                is_coastal = yes
            }
        }
        priority = 8000
        ordertype = front
        execution_type = rush
        execute_order = yes
        manual_attack = yes
    }

}

moren_wartime_air_superiority = {#全力争夺制空权
    allowed = { NOT = {
        OR = {
            original_tag = PAO
            original_tag = PAM
            original_tag = ELF
            original_tag = KNM
            original_tag = KFL
            original_tag = LHS
        }
    } }
    enable = {
        has_war = yes
        num_of_military_factories > 50
        OR = {
            has_tech = basic_small_airframe
        }
    }
    abort_when_not_enabled = yes

    #大幅提升战斗机产能
    ai_strategy = {
        type = equipment_production_factor
        id = fighter
        value = 150
    }
    ai_strategy = {
        type = equipment_production_min_factories
        id = fighter
        value = 12
    }
    #CAS配合地面攻势
    ai_strategy = {
        type = equipment_production_factor
        id = cas
        value = 100
    }
    ai_strategy = {
        type = equipment_production_min_factories
        id = cas
        value = 8
    }
    #提升空军工厂分配
    ai_strategy = {
        type = air_factory_balance
        value = 15
    }

}




moren_crisis_iron_wall = {
    allowed = { NOT = {
        OR = {
            original_tag = PAO
            original_tag = PAM
            original_tag = ELF
            original_tag = KNM
            original_tag = KFL
            original_tag = LHS
        }
    } }
    enable = {
        surrender_progress > 0.20
        has_war = yes
        not = {
            has_war_with = PAU
        }
        not = {
            has_war_with = PAM
        }
    }
    abort = {
        OR = {
            surrender_progress < 0.13
            has_capitulated = yes
        }
    }

    #全面转为防守
    ai_strategy = {
        type = front_control
        state_trigger = {
            is_controlled_by = ROOT
        }
        priority = 20000
        ordertype = front
        execution_type = careful
        manual_attack = yes
    }
    #大量补充部队到核心州防线
    ai_strategy = {
        type = front_unit_request
        state_trigger = {
            is_core_of = ROOT
            is_controlled_by = ROOT
            any_neighbor_state = {
                controller = { has_war_with = ROOT }
            }
        }
        value = 200
    }
    #抽回非核心区的兵力
    ai_strategy = {
        type = front_unit_request
        state_trigger = {
            NOT = { is_core_of = ROOT }
            NOT = {
                any_province_building_level = {
                    province = { all_provinces = yes }
                    building = supply_node
                    level > 0
                }
            }
        }
        value = -100
    }
    #大幅提升步枪和炮兵补充
    ai_strategy = {
        type = equipment_production_factor
        id = infantry
        value = 300
    }
    ai_strategy = {
        type = equipment_production_factor
        id = artillery
        value = 200
    }
    #停止装甲生产先活下去
    ai_strategy = {
        type = equipment_production_factor
        id = armor
        value = -200
    }
    #增加驻军保住占领地
    ai_strategy = {
        type = garrison
        value = 100
    }
    #爆兵
    ai_strategy = {
        type = ai_wanted_divisions_factor
        value = 2000
    }
}

moren_capital_shield = {
    allowed = { NOT = {
        OR = {
            original_tag = PAO
            original_tag = PAM
            original_tag = ELF
            original_tag = KNM
            original_tag = KFL
            original_tag = LHS
        }
    } }
    enable = {
        NOT = {
            any_enemy_country = { surrender_progress > 0.3 }
        }
    }
    abort_when_not_enabled = yes
    #首都及其周边州请求额外部队
    ai_strategy = {
        type = front_unit_request
        state_trigger = {
            OR = {
                is_capital = yes
                AND = {
                    any_neighbor_state = { is_capital = yes }
                    is_controlled_by = ROOT
                }
            }
        }
        value = 80
    }
    #首都周边执行谨慎模式
    ai_strategy = {
        type = front_control
        state_trigger = {
            any_neighbor_state = { is_capital = yes }
            is_controlled_by = ROOT
            any_neighbor_state = {
                controller = { has_war_with = ROOT }
            }
        }
        priority = 25000
        ordertype = front
        execution_type = careful
        manual_attack = yes
    }
}
#识别可被包围的敌军并优先切断
moren_encirclement_hunter = {
    allowed = { NOT = {
        OR = {
            original_tag = PAO
            original_tag = PAM
            original_tag = ELF
            original_tag = KNM
            original_tag = KFL
            original_tag = LHS
        }
    } }
    enable = {
        has_war = yes
        num_of_military_factories > 60
        num_divisions > 200
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = front_control
        state_trigger = {
            controller = { has_war_with = ROOT }
            NOT = { is_coastal = yes }
            any_neighbor_state = { is_controlled_by = ROOT }
            check_variable = { num_enemy_neighbor_states < 2 }
        }
        priority = 25000
        ordertype = front
        execution_type = rush_weak
        execute_order = yes
        manual_attack = yes
    }
    ai_strategy = {
        type = front_unit_request
        state_trigger = {
            controller = { has_war_with = ROOT }
            NOT = { is_coastal = yes }
            check_variable = { num_enemy_neighbor_states < 2 }
        }
        value = 60
    }
}
moren_end_pocket = {#包围快速歼灭
    allowed={
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        always = yes
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = front_control
        state_trigger = {
            OR = {
                AND = {
                    is_controlled_by = FROM.FROM
                    NOT = {
                        is_fully_controlled_by = FROM.FROM
                    }
                }
                controller = {
                    has_war_with = FROM.FROM
                }
            }
            all_neighbor_state = {
                controller = {
                    NOT = {
                        has_war_with = FROM.FROM
                    }
                }
            }
        }
        ratio = 0.0
        priority = 9999999
        ordertype = front
        execution_type = rush_weak
        execute_order = yes
        manual_attack = yes

    }

    ai_strategy = {

        type = front_unit_request

        state_trigger = {
            OR = {
                AND = {
                    is_controlled_by = FROM.FROM
                    NOT = {
                        is_fully_controlled_by = FROM.FROM
                    }
                }
                controller = {
                    has_war_with = FROM.FROM
                }
            }
            all_neighbor_state = {
                controller = {
                    NOT = {
                        has_war_with = FROM.FROM
                    }
                }
            }
        }
        value = 40
    }
}
moren_concentrate_troops = {#集中突破
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        always = yes
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = force_concentration_factor
        value = 15  # base + 15 = 30 %
    }

}
moren_victory_phase1_crack = {
    allowed = { NOT = {
        OR = {
            original_tag = PAO
            original_tag = PAM
            original_tag = ELF
            original_tag = KNM
            original_tag = KFL
            original_tag = LHS
        }
    } }
    enable = {
        has_war = yes
        any_enemy_country = {
            surrender_progress > 0.05
            surrender_progress < 0.20
        }
        NOT = { surrender_progress > 0.15 }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = front_control
        state_trigger = {
            controller = { has_war_with = ROOT }
            any_province_building_level = {
                province = { all_provinces = yes }
                building = supply_node
                level > 0
            }
            any_neighbor_state = { is_controlled_by = ROOT }
        }
        priority = 12000
        ordertype = front
        execution_type = rush
        execute_order = yes
        manual_attack = yes
    }
    ai_strategy = {
        type = garrison
        value = -150
    }
    ai_strategy = {
        type = spare_unit_factor
        value = -20
    }
}

moren_victory_phase2_exploit = {
    allowed = { NOT = {
        OR = {
            original_tag = PAO
            original_tag = PAM
            original_tag = ELF
            original_tag = KNM
            original_tag = KFL
            original_tag = LHS
        }
    } }
    enable = {
        has_war = yes
        any_enemy_country = {
            surrender_progress > 0.20
            surrender_progress < 0.45
        }
        NOT = { surrender_progress > 0.15 }
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = front_control
        state_trigger = {
            owner = {
                has_war_with = ROOT
                surrender_progress > 0.20
            }
            is_capital = yes
            any_neighbor_state = { is_controlled_by = ROOT }
        }
        priority = 40000
        ordertype = front
        execution_type = rush
        execute_order = yes
        manual_attack = yes
    }
    ai_strategy = {
        type = front_control
        state_trigger = {
            controller = {
                has_war_with = ROOT
                surrender_progress > 0.20
            }
        }
        priority = 15000
        ordertype = front
        execution_type = rush_weak
        execute_order = yes
        manual_attack = yes
    }
    ai_strategy = {
        type = spare_unit_factor
        value = -40
    }
    ai_strategy = {
        type = garrison
        value = -200
    }
}

moren_victory_phase3_annihilate = {
    allowed = { NOT = {
        OR = {
            original_tag = PAO
            original_tag = PAM
            original_tag = ELF
            original_tag = KNM
            original_tag = KFL
            original_tag = LHS
        }
    } }
    enable = {
        has_war = yes
        any_enemy_country = { surrender_progress > 0.45 }
        NOT = { surrender_progress > 0.15 }
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = front_control
        state_trigger = {
            controller = {
                has_war_with = ROOT
                surrender_progress > 0.45
            }
        }
        priority = 60000
        ratio = 0.95
        ordertype = front
        execution_type = rush_weak
        execute_order = yes
        manual_attack = yes
    }

    # 全军压上
    ai_strategy = {
        type = spare_unit_factor
        value = -100
    }
    ai_strategy = {
        type = garrison
        value = -300
    }
    # 爆更多师团填线
    ai_strategy = {
        type = ai_wanted_divisions_factor
        value = 300
    }
}
moren_strategic_withdrawal = {#主动收缩
    allowed = { NOT = {
        OR = {
            original_tag = PAO
            original_tag = PAM
            original_tag = ELF
            original_tag = KNM
            original_tag = KFL
            original_tag = LHS
        }
    } }
    enable = {
        has_war = yes
        surrender_progress > 0.05
        surrender_progress < 0.20
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = front_unit_request
        state_trigger = {
            is_controlled_by = ROOT
            NOT = { is_core_of = ROOT }
            NOT = {
                any_province_building_level = {
                    province = { all_provinces = yes }
                    building = supply_node
                    level > 0
                }
            }
            NOT = { is_capital = yes }
            any_neighbor_state = { controller = { has_war_with = ROOT } }
        }
        value = -150
    }

    ai_strategy = {
        type = front_unit_request
        state_trigger = {
            is_core_of = ROOT
            is_controlled_by = ROOT
            any_neighbor_state = { controller = { has_war_with = ROOT } }
        }
        value = 150
    }

    ai_strategy = {
        type = front_control
        state_trigger = {
            is_core_of = ROOT
            is_controlled_by = ROOT
            any_neighbor_state = { controller = { has_war_with = ROOT } }
        }
        priority = 10000
        ordertype = front
        execution_type = careful
        manual_attack = yes
    }
}

moren_protect_supply_lines = { #死守补给点
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        has_war = yes
    }
    abort_when_not_enabled = yes


    ai_strategy = {
        type = front_control
        state_trigger = {
            is_controlled_by = ROOT
            any_province_building_level = {
                province = { all_provinces = yes }
                building = supply_node
                level > 0
            }

            any_neighbor_state = {
                controller = { has_war_with = ROOT }
            }
        }
        priority = 5000
        ordertype = front
        execution_type = careful
        manual_attack = yes
    }


    ai_strategy = {
        type = front_unit_request
        state_trigger = {
            is_controlled_by = ROOT
            any_province_building_level = {
                province = { all_provinces = yes }
                building = supply_node
                level > 0
            }
            any_neighbor_state = {
                controller = { has_war_with = ROOT }
            }
        }
        value = 50
    }
}

moren_check_omega_lul = { #部队逻辑
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        always = yes
    }
    abort = {
        always = no
    }
    ai_strategy = {#不待烂地
        type = front_control
        state_trigger = {
            is_controlled_by = ROOT
            NOT = {
                any_province_building_level = {
                    province = { all_provinces = yes }
                    building = supply_node
                    level > 0
                }
            }
            NOT = { is_capital = yes }
        }
        priority = 10
        ordertype = front
        execution_type = careful
    }

    #告诉AI从这些垃圾地块把兵抽走
    ai_strategy = {
        type = front_unit_request
        state_trigger = {
            is_controlled_by = ROOT
            NOT = { #没有补给站
                any_province_building_level = {
                    province = { all_provinces = yes }
                    building = supply_node
                    level > 0
                }
            }
            NOT = { is_capital = yes }
        }
        value = -50
    }
    ai_strategy = {
        type = front_unit_request
        state_trigger = {
            is_capital = yes
            owner = { tag = FROM.FROM }
        }
        value = 50
    }


    ai_strategy = { #登陆更有价值的地块
        type = invasion_unit_request
        state_trigger = {
            NOT = { has_state_flag = recent_naval_invasion_flag }
            check_variable = { state_strategic_value < 3 }
        }
        value = -5
    }
    ai_strategy = {
        type = invasion_unit_request
        state_trigger = {
            NOT = { has_state_flag = recent_naval_invasion_flag }
            NOT = { is_core_of = FROM.FROM }
            check_variable = { state_strategic_value < 8 }
        }
        value = -3
    }
    ai_strategy = {
        type = invasion_unit_request
        state_trigger = {
            NOT = { has_state_flag = recent_naval_invasion_flag }
            NOT = { is_core_of = FROM.FROM }
        }
        value = -3
    }
    ai_strategy = {
        type = invasion_unit_request
        state_trigger = {
            NOT = { has_state_flag = recent_naval_invasion_flag }
            NOT = { is_core_of = FROM.FROM }
            NOT = {
                any_province_building_level = {
                    province = {
                        all_provinces = yes
                    }
                    building = supply_node
                    level > 0
                }
            }
        }
        value = -15
    }
    ai_strategy = {
        type = invasion_unit_request
        state_trigger = {
            NOT = { has_state_flag = recent_naval_invasion_flag }

            any_province_building_level = {
                province = {
                    all_provinces = yes
                }
                building = supply_node
                level > 0
            }

        }
        value = 15
    }

}
moren_build_an_air_facility_faster = {#建造空军研究设施
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {

        is_major = yes # Minors don't bother until strong enough to be considered major
        num_of_military_factories > 40 # We should first build up our military concentrated_industry

        OR = { # Don't start until we can build decent aircraft
            has_tech = basic_small_airframe
            has_tech = basic_medium_airframe
            has_tech = basic_large_airframe
        }
        air_facility < 1 # one is enough
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = build_building
        id = air_facility
        value = 1000
    }
}

moren_build_a_lw_facility_faster = {#建造陆军研究设施
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        is_major = yes
        num_of_civilian_factories > 40
        num_of_military_factories > 40
        OR = { # Don't start until we can build decent tanks of some kind
            has_tech = improved_light_tank_chassis
            has_tech = basic_medium_tank_chassis
            has_tech = improved_heavy_tank_chassis
        }
        land_facility < 1 # one is enough
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = build_building
        id = land_facility
        value = 1000
    }
}





moren_build_a_nav_facility_faster = {#建造海军研究设施
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        num_of_naval_factories > 25 # Only Naval Powers should bother with these unless specifically told to do otherwise
        naval_facility < 1 # one is enough
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = build_building
        id = naval_facility
        value = 1000
    }
}


moren_build_a_nl_facility_faster = {#建造核研究设施
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        has_tech = atomic_research
        num_of_civilian_factories > 25
        num_of_military_factories > 25 # Even if we have the base tech, let's wait until we have some kind of capacity
        nuclear_facility < 1  # one is enough
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = build_building
        id = nuclear_facility
        value = 1000
    }
}

LHS_recruit_land_scientist_faster = { #招募陆战科学家
    allowed = {
        original_tag = LHS
    }
    enable = {
        land_facility > 0
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = recruit_scientist
        id = specialization_land
        value = 1000
    }
}

moren_recruit_nuclear_scientist_faster = {#招募核研究科学家
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        nuclear_facility > 0
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = recruit_scientist
        id = specialization_nuclear
        value = 1000
    }
}

moren_recruit_naval_scientist_faster = {#招募海战科学家
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        naval_facility > 0
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = recruit_scientist
        id = specialization_naval
        value = 1000
    }
}

moren_recruit_air_scientist_faster = {#招募空战科学家
    allowed = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    enable = {
        air_facility > 0
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = recruit_scientist
        id = specialization_air
        value = 1000
    }
}
#空军生产
early_air_production_basic = {
    enable = {
        NOT = {
            OR = {
                original_tag = PAO
                original_tag = PAM
                original_tag = ELF
                original_tag = KNM
                original_tag = KFL
                original_tag = LHS
            }
        }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type  = unit_ratio
        id    = fighter
        value = 40
    }
    ai_strategy = {
        type  = unit_ratio
        id    = cas
        value = 20
    }
    ai_strategy = {
        type  = unit_ratio
        id    = tactical_bomber
        value = 10
    }
    ai_strategy = {
        type  = unit_ratio
        id    = strategic_bomber
        value = 0
    }
    ai_strategy = {
        type  = unit_ratio
        id    = scout_plane
        value = 5
    }
    ai_strategy = {
        type  = unit_ratio
        id    = maritime_patrol_plane
        value = 10
    }
}
#海军生产
ai_naval_unit_role_ratios = {
    allowed = {
    }
    enable = { num_of_naval_factories > 0 }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = role_ratio
        id = naval_capital_bb
        value = -20 # 40
    }

    ai_strategy = {
        type = role_ratio
        id = naval_capital_bc
        value = -20
    }

    ai_strategy = {
        type = role_ratio
        id = naval_carrier
        value = -15 # 35
    }

    ai_strategy = {
        type = role_ratio
        id = naval_submarine
        value = 50
    }

    ai_strategy = {
        type = role_ratio
        id = naval_better_screen
        value = 40
    }

    ai_strategy = {
        type = role_ratio
        id = naval_escort
        value = 25
    }

    ai_strategy = {
        type = role_ratio
        id = naval_cruiser_light
        value = 15
    }

    ai_strategy = {
        type = role_ratio
        id = naval_cruiser_heavy
        value = 10
    }

    ai_strategy = {
        type = role_ratio
        id = naval_mine_sweeper
        value = 10
    }

    ai_strategy = {
        type = role_ratio
        id = naval_mine_layer
        value = 15
    }
}


we_need_convoys = {
    enable = { stockpile_ratio = { archetype = convoy ratio < 75 } }
    abort = { stockpile_ratio = { archetype = convoy ratio > 125 } }

    ai_strategy = {
        type = equipment_production_min_factories
        id = convoy
        value = 5
    }
}

need_convoys2 = {
    enable = { stockpile_ratio = { archetype = convoy ratio < 150 } }
    abort = { stockpile_ratio = { archetype = convoy ratio > 200 } }

    ai_strategy = {
        type = equipment_production_min_factories
        id = convoy
        value = 5
    }
}

need_convoys3 = {
    enable = { stockpile_ratio = { archetype = convoy ratio < 250 } }
    abort = { stockpile_ratio = { archetype = convoy ratio > 300 } }

    ai_strategy = {
        type = equipment_production_min_factories
        id = convoy
        value = 3
    }
}

need_convoys4 = {
    enable = { stockpile_ratio = { archetype = convoy ratio < 400 } }
    abort = { stockpile_ratio = { archetype = convoy ratio > 450 } }

    ai_strategy = {
        type = equipment_production_min_factories
        id = convoy
        value = 2
    }
}

dont_need_convoys_1 = {
    enable = { stockpile_ratio = { archetype = convoy ratio > 200 } }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_variant_production_factor
        id = convoy
        value = -500
    }
}

dont_need_convoys_2 = {
    enable = { stockpile_ratio = { archetype = convoy ratio > 500 } }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_variant_production_factor
        id = convoy
        value = -500
    }
}

default_produce_floating_harbors = {
    enable = {
        has_war = yes
        num_of_naval_factories > 25
        num_of_military_factories > 40 # floating harbors are used for invasions. Why do large invasions if you don't have the industrial capacity?
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = unit_ratio
        id = floating_harbor # for floating harbors, 'unit_ratio' is actually not a ratio but an absolute desired number
        value = 10
    }
}

#ai行为
default_pp_spend_amount = {
    enable = {
        always = yes
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = pp_spend_amount
        id = idea
        value = 100
    }
    ai_strategy = {
        type = pp_spend_amount
        id = decision
        value = 100
    }
    ai_strategy = {
        type = equipment_production_min_factories_archetype
        id = super_heavy_artillery_equipment
        value = -99
    }
    ai_strategy = {
        type = equipment_variant_production_factor
        id = super_heavy_artillery_equipment
        value = -99
    }

    ai_strategy = {
        type = super_heavy_artillery_equipment
        value = -999
    }
    ai_strategy = {
        type = equipment_production_min_factories_archetype
        id = self_propelled_super_heavy_artillery_equipment
        value = -99
    }
    ai_strategy = {
        type = equipment_variant_production_factor
        id = self_propelled_super_heavy_artillery_equipment
        value = -99
    }

    ai_strategy = {
        type = self_propelled_super_heavy_artillery_equipment
        value = -999
    }
    ai_strategy = {
        type = pp_spend_priority
        id = relation
        value = -9999
    }
    ai_strategy = {
        type = pp_spend_priority
        id = relation
        value = -9999
    }
}

dont_guard_against_overlord_puppets = {
    enable = {
        is_subject = yes
        is_in_faction = no
        overlord = { num_subjects > 1 }
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = front_unit_request
        country_trigger = {
            is_subject = yes
            overlord = { has_subject = ROOT }
        }
        value = -2000
    }
}

ask_foreign_manpower_as_major = {
    enable = {
        is_major = yes
        garrison_manpower_need > 200000
    }

    abort = {
        garrison_manpower_need < 100000
    }

    ai_strategy = {
        type = asking_foreign_garrison
        value = 100
        # id = ITA # id is optionnal. If it is filled, the value will just apply for this country. If not, the value will apply for all countries.
    }
}

put_garrisons_on_high_prio = {
    enable = {
        always = yes
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = garrison_reinforcement_priority
        value = 100 # positive is high prio, negative is low prio
    }
}

division_upgrades_are_very_important = {
    enable = {
        has_war = yes
        NOT = {
            original_tag = PAO
            original_tag = LHS
            original_tag = PAM
        }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = land_xp_spend_priority
        id = division_template
        value = 100
    }
}

